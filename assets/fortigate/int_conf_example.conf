config sys int
  edit port1
    set mode static
    set alias FGTs_UCS
    set ip 10.0.18.3/24
    set allowaccess ping, https, http, ssh
  next
  edit port2
    set mode static
    set alias WAN 
    set allowaccess https http ssh ping
end